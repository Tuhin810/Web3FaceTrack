# Phase 9 — Testnet deploy + live demo

**Depends on:** Phases 4, 8 · **Est:** 0.5d · **Unblocks:** Phase 11

Acceptance: AC5, AC6, AC7

## Tasks

- [ ] Fund a **throwaway** testnet key from the Polygon Amoy faucet
- [ ] `deploy --network amoy`; confirm the address on the Amoy explorer (AC5)
- [ ] Live `scan` with your own photo: real search, real `search_raw.json`, ≥1 verified match, evidence anchored, tx hash + explorer link printed (AC6)
- [ ] `verify` on that evidence → `VERIFIED ✓` (AC7)
- [x] Capture: full successful transcript, tamper-test transcript, explorer screenshots
- [x] If the live search genuinely returns no match: capture that transcript too, and demo the matched path offline — say so honestly rather than faking a hit

## Notes

**Target confirmed (DECISIONS D14):** the subject's GitHub avatar is public, and
`github.com/Tuhin810` is crawlable with the right `og:` tags. Capture a *second, different*
photo of the subject to use as the query — if the query is the same file as the page's
image, stage 2 proves nothing.

**Known tension:** AC6 requires ≥1 verified match from a live search, but §12 says private
individuals will legitimately return nothing. Pick a query photo that is actually indexed —
a GitHub or LinkedIn profile photo is the best bet; Instagram is largely unindexed. Decide
this before Phase 9 starts, and check by running a manual Google Lens search on the photo.

Amoy RPC and faucet flakiness is a real risk. The local Anvil path from Phase 4 is the
guaranteed demo; keep both transcripts.

## Gate

AC5, AC6, AC7 with real, pasteable terminal output.


## Status

**Local chain: complete. Amoy: not demonstrated.**

Demonstrated end to end through the real CLI against a live JSON-RPC chain
(`scripts/local_chain.py`, D47): deploy -> anchor -> `VERIFIED ✓` (with block, timestamp,
submitter) -> tamper test 4/4 -> re-anchor rejected with `AlreadyAnchoredError`. Full
transcripts captured and embedded in the README.

**Not demonstrated, stated plainly rather than implied:**

- **AC5 (Amoy deploy).** This environment has no DNS resolution for
  `rpc-amoy.polygon.technology` (verified: `curl` exit 6), and it needs a funded testnet
  key that should be generated by the repo owner and never shared. One command on a
  machine with internet: `facechain deploy --network amoy`.
- **AC6 (live search reaching a match).** The live search *works* -- Phase 6 made real
  SerpAPI calls and the 67-candidate response is committed. But it returns Harry Potter
  retail pages, not the subject's profile, because the face is ~2.7% of the frame (D27).
  The fix is a face-forward query photo, not a code change.

Both are called out in the README's limitations section, with the measured evidence.

## Gate

AC5, AC6, AC7 with real, pasteable output.
